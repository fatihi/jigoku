name: Deploy Game Server

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  test:
    uses: ./.github/workflows/build.yml

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set build version
        id: version
        run: echo "BUILD_VERSION=${{ github.run_number }}-$(date +%Y-%m-%d)" >> "$GITHUB_OUTPUT"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}
          build-args: BUILD_VERSION=${{ steps.version.outputs.BUILD_VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy to Lightsail
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.LIGHTSAIL_HOST }}
          username: ${{ secrets.LIGHTSAIL_USER }}
          key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          script: |
            cd /opt/jigoku
            docker compose -f docker-compose.prod.yml pull gamenode-1 gamenode-2
            docker compose -f docker-compose.prod.yml up -d gamenode-1 gamenode-2
            docker image prune -f

  cleanup:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Delete old container images
        continue-on-error: true
        uses: actions/delete-package-versions@v5
        with:
          package-name: jigoku
          package-type: container
          min-versions-to-keep: 3
          delete-only-untagged-versions: false
